<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Quiz</title>
    <!-- Bootstrap CSS -->
    <link href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">
    
<div class="container mt-5">
    <div class="card">
        <div class="card-header">
            <h2>Quiz</h2>
        </div>
        <div class="card-body">
            <h5 class="card-title">Question <%= questionNumber %> of <%= totalQuestions %>: <%= question.question %></h5>

            <form action="/" method="post">
                <% for(let choice of question.choices) { %>
                <div class="form-check">
                    <input class="form-check-input" type="radio" name="answer" id="<%= choice %>" value="<%= choice %>" <%= (choice == selected_answer) ? "checked" : "" %>>
                    <label class="form-check-label" for="<%= choice %>">
                        <%= choice %>
                    </label>
                </div>
                <% } %>
                <br>
                <button type="submit" name="submit" value="Submit Answer" class="btn btn-primary">Submit Answer</button>
                <button type="submit" name="submit" value="Clear" class="btn btn-danger">Clear</button>
                <button type="submit" name="submit" value="Back" class="btn btn-secondary">Back</button>
                <button type="submit" name="submit" value="Next" class="btn btn-secondary">Next</button>
            </form>

            <p class="mt-3"><strong><%= message %></strong></p>
            <p><%= explanation %></p>
            <p class="mt-3"><strong>Your score: <%= userScore %> out of <%= totalQuestions %></strong></p>
        </div>
    </div>
</div>

<!-- Optional Bootstrap JavaScript -->
<script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
<script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.3/dist/umd/popper.min.js"></script>
<script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>

</body>
</html>
